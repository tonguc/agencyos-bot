Doctor searches could rank a residential development named DOKTORLAR SITESI as a clinical prospect. Reject explicit housing categories in healthcare searches while retaining hospitals, and reject coordinates over 50 km from a known requested district. Missing address text alone remains unverified rather than excluded. Version caches to discard previous search results.

The map painted percentage-positioned buttons over an independently pannable OSM iframe. Replace those detached overlays with the map's own marker for the selected candidate, plus a candidate selector. The map now displays one candidate at a time and does not imply a synchronized multi-marker view.

Validation: 125 backend tests passed (DB integration suites excluded), TypeScript and targeted ESLint passed. Includes residential false positives, retained doctors/hospitals, and distant versus incomplete locations. No scoring weights or database records changed.
